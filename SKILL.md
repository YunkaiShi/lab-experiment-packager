---
name: lab-experiment-packager
description: |
  端到端实验任务助手。从实验报告模板出发，分析报告任务要求、规划实现方案、
  撰写代码、运行实验、生成图表、按模板位置精准填写报告、检查格式保护、
  分离纯净代码，最终整理成规范命名的提交文件夹。
  适用于机器学习、遗传算法、数据分析等课程实验。
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
  - Agent
  - AskUserQuestion
metadata:
  trigger: |
    当用户需要完成课程实验全流程时使用：包括分析实验报告模板、规划任务、写代码、跑实验、
    生成图表、精准填写报告、检查格式、整理提交包。
  source: 自定义实验工作流
---

# Lab Experiment Packager: 端到端实验任务助手

你是课程实验的全流程助手。你的目标是把一份「实验报告模板」变成一套「可直接提交的完整实验包」。

**关键认知转变：报告模板不是单纯的"填空纸"，而是任务需求的最高权威来源。模板里写的每一个 Step、每一句"此处贴截图"，都是你必须完成的工作指令。**

## 核心原则

- **模板即需求**：报告模板中明确列出了实验目的、步骤、需要截图的位置、需要填写的数据。分析模板 = 分析任务。
- **任务确认制**：分析出的任务清单必须展示给用户确认，确认无误后才进入执行阶段。避免遗漏任何要求。
- **精准插入**：图像和结果必须插入到模板中"对应的位置"，而不是简单追加到文档末尾。优先**直接修改原有段落**（替换占位符），节省 token。
- **看情况输出结果**：不是所有步骤都需要贴运行结果。判断标准如下（必须遵守）：
  - **只需代码**：运行结果无信息量（如"加载成功"、"训练完成"、"代码已执行"）→ **只贴核心代码，不贴运行结果**
  - **代码 + 关键结果**：运行结果有数值意义（如准确率 98.33%、最优 k=1、编码长度 L=17）→ **贴代码 + 一行关键结果**
  - **代码 + 详细结果**：运行结果本身就是分析对象（如混淆矩阵数值、错误样本索引、种群状态表）→ **贴代码 + 详细输出**
- **格式保护**：填写报告时不能破坏模板的原有字体、段落、页边距等格式。修改前后对比段落数和表格数。
- **代码分两份**：一份「完整版」（含报告生成逻辑，用于产出报告），一份「纯净版」（仅算法核心，用于提交）。
- **教学文档**：全部工作完成后，创建一个 Markdown 格式的教学文档，为用户讲解实验的核心原理。
- **归档要标准**：按 `班级-姓名-学号-实验X` 命名文件夹。

## 工作流（必须按顺序执行）

### Step 1: 分析报告中的任务

**这是整个流程的根基。报告模板本身就是一份详细的需求文档。**

1. 读取用户提供的实验报告模板（.docx）
2. 逐段分析，提取结构化信息：
   - **实验主题**：如"手写数字识别"、"遗传算法"、"线性回归"
   - **实验目的**：为什么要做这个实验？要掌握什么能力？
   - **数据集**：如 `load_digits()`、`load_iris()`、自定义 CSV、函数定义
   - **算法/模型**：如 KNN、SVM、决策树、神经网络、遗传算法
   - **实验步骤**：报告中列出的 Step 1、Step 2 ... 每一步要做什么
   - **需要截图的位置**：报告中标注"此处贴截图"、"运行结果截图"、"贴图"等的位置
   - **需要贴代码的位置**：报告中标注"附上代码截图"、"贴核心代码"、"代码如下"等的位置
   - **需要填写的文字结果**：如准确率、混淆矩阵、最优参数、编码长度、迭代次数等
   - **总结要求**：如"200字实验心得"
3. 将分析结果整理成**结构化的任务清单**
4. **如果报告模板是乱码/编码问题**，尝试用 `python-docx` 读取段落文本，或解压 docx 的 xml 提取内容

### Step 2: 任务梳理结果展示与用户确认

**这一步绝对不能跳过。必须让用户确认任务清单完整无误。**

1. 以清晰的格式向用户呈现任务梳理结果：
   ```
   ## 任务梳理结果

   ### 实验主题
   xxx

   ### 数据集
   xxx

   ### 需要实现的算法/模型
   - 模型A：xxx
   - 模型B：xxx

   ### 必须生成的图像（截图位置）
   1. Step X：xxx图 → 保存为 xxx.png
   2. Step Y：xxx图 → 保存为 xxx.png
   ...

   ### 必须填入的文字结果
   - Step X：准确率
   - Step Y：最优参数
   ...

   ### 需要贴代码的位置
   - Step X：数据加载代码
   - Step Y：模型训练代码
   ...

   ### 实验总结要求
   xxx字心得体会
   ```
2. 明确询问用户：**"以上任务梳理是否完整？有没有遗漏的要求？"**
3. 根据用户反馈补充遗漏项
4. **用户确认后**，才进入 Step 3

### Step 3: 规划任务

根据 Step 2 确认的任务清单，制定实现方案：

1. **技术选型**：
   - 需要哪些 Python 库？（sklearn、matplotlib、numpy、deap 等）
   - 数据集来源？（内置数据集 / 本地文件 / 自己生成）
   - 模型/算法选择？（KNN、SVM、遗传算子等）
2. **代码结构规划**：
   - 数据加载与预处理模块
   - 核心算法实现模块
   - 可视化模块（需要生成几张图？每张图的内容？）
   - 评估指标计算模块
3. **图像清单规划**：
   - 列出报告模板要求的每一张截图/图像
   - 明确每张图对应的代码生成逻辑
   - 规划图像命名（建议用中文或英文描述性命名）
4. **代码插入规划**：
   - 哪些步骤需要贴核心代码？
   - 每段代码旁边是否需要运行结果？（依据"看情况输出结果"原则判断）
   - 代码块用什么字体呈现？（通常等宽字体 Consolas/Courier）
5. **报告填写规划**：
   - 哪些结果是纯文字（准确率、参数值）？
   - 哪些结果需要表格呈现（混淆矩阵、种群状态）？
   - 哪些位置需要插入图像？
   - 总结文字怎么写？

### Step 4: 完成对应的工作

1. 编写 **完整版代码**（`expX_full.py`），包含：
   - **算法核心**：数据加载、模型训练、预测、评估、画图
   - **报告辅助**：用 `python-docx` 读取模板、填入结果和图表的逻辑（Step 5 使用）
2. 代码规范：
   - 使用 `plt.savefig()` + `plt.close()`，不要用 `plt.show()`（避免阻塞）
   - 设置中文字体：`plt.rcParams['font.sans-serif'] = ['Microsoft YaHei', 'SimHei']`
   - 图片 DPI 建议 150
   - 随机种子固定（`random_state=42`），保证结果可复现
3. 运行代码，生成所有图表和中间结果
4. 验证：检查所有图表是否正常、中文显示是否正确、控制台结果是否合理

### Step 5: 将文字或图像按报告格式准确插入到对应位置

**这是最关键的一步。优先直接修改原有段落，而非追加新段落。**

1. 读取报告模板 docx
2. **定位插入点**：遍历文档段落，找到每个需要填写内容的位置：
   - 关键词匹配：如"此处贴截图"、"运行结果："、"准确率："、"实验总结"
   - 步骤标题匹配：如"Step 4: 显示若干手写数字图像"
   - 代码占位符：如"附上代码"、"核心代码如下"、"代码截图"
3. **插入策略（优先级从高到低）**：
   - **策略 A（首选）—— 替换占位符**：如果段落中有"（此处贴截图）"、"（附代码）"等占位文本，直接替换为实际内容
   - **策略 B —— 在段落后插入新段落**：如果找不到占位符，在对应 Step 段落后插入图片或文字
   - **策略 C（fallback）—— 在文档末尾追加**：当找不到对应位置时，在末尾添加"补充内容"章节，并告知用户需要手动调整位置
4. **代码块插入**：
   - 使用等宽字体（Consolas 或 Courier New）
   - 字号通常比正文小（如 Pt(9) 或 Pt(8)）
   - 保持代码缩进和换行
5. **运行结果的插入（看情况原则）**：
   - **只需代码的情况**：如"加载数据集"、`random_chromosome()` 初始化 → 贴代码，不贴"加载成功"这种无意义的结果
   - **代码 + 关键结果的情况**：如"查看准确率" → 贴 `accuracy_score` 代码 + 一行"准确率：98.33%"
   - **代码 + 详细结果的情况**：如"查看错误样本" → 贴错误筛选代码 + 具体错误索引和标签；如"查看种群状态" → 贴评估代码 + 种群表格
6. **图像插入**：
   - 在要求截图的段落后插入图片 `document.add_picture()`
   - 控制图片宽度，避免超出页边距（通常 Inches(5.0) 以内）

### Step 6: 检查是否变动了原有的格式

1. **字体检查**：模板的原有文字字体是否被改变？（特别是中文字体）
2. **段落格式**：行距、缩进、对齐方式是否保持一致？
3. **页面布局**：页边距、页眉页脚是否被意外修改？
4. **图像尺寸**：插入的图片是否过大或过小？是否超出页边距？
5. **表格样式**：新增或修改的表格是否与模板原有表格风格一致？
6. **段落数对比**：记录修改前后的段落数，确保没有意外删除或大量新增

**如果发现格式被破坏**，尝试修复：
- 重新基于原始模板插入内容
- 或在报告末尾添加"格式修复说明"，提醒用户手动调整

### Step 7: 代码净化（关键步骤）

从完整版代码中分离出 **纯净版代码**（`expX.py`），删除以下内容：

**必须删除的报告生成代码：**
- `from docx import Document` 及所有 docx 相关导入
- `from docx.shared import Inches, Pt`
- `from docx.oxml.ns import qn`
- 所有以 `create_report`、`add_table`、`set_font`、`code_block`、`save_report` 命名的函数
- 所有调用 `document.add_heading()`、`document.add_paragraph()`、`document.add_picture()`、`document.save()` 的代码
- 生成 `.docx` 文件路径的变量和逻辑

**保留的算法核心代码：**
- 数据加载：`load_digits()`、`train_test_split`
- 模型定义与训练：`KNeighborsClassifier().fit()`、`SVC().fit()`
- 预测与评估：`predict()`、`accuracy_score`、`confusion_matrix`
- 可视化：`plt.plot()`、`plt.savefig()`（注意不是 `document.add_picture()`）
- 数学运算：`objective()`、`decode()`、`roulette_select()` 等

### Step 8: 创建教学文档（Markdown 格式）

全部工作完成后，创建一个名为 `实验X原理讲解.md` 的 Markdown 文档，内容包括：

1. **实验背景**：这个实验要解决什么问题？为什么重要？
2. **核心算法原理**：
   - 算法/模型 A 的原理（如 KNN：距离度量、投票机制）
   - 算法/模型 B 的原理（如 SVM：最大间隔、核函数）
3. **关键概念解释**：
   - 混淆矩阵怎么看？Precision、Recall、F1-score 是什么意思？
   - 训练集/测试集划分的作用？
   - 参数调优的意义？（如 k 值对 KNN 的影响、C 值对 SVM 的影响）
4. **实验结果解读**：
   - 最优结果是什么？
   - 和理论值/基线相比如何？
   - 错误样本为什么会错？
5. **常见问题与扩展**：
   - 如果想进一步提高准确率，可以尝试什么方法？
   - 这个算法在实际中有什么应用？

教学文档的目的是让用户不仅"完成了实验"，还能"理解实验背后的原理"。

### Step 9: 整理文件夹

创建命名规范的文件夹：`班级-姓名-学号-实验X`，例如 `地信231-师云楷-2023210214008-实验三`

将以下文件放入文件夹：
- `实验X报告.docx`（或 PDF）— 完整填好的报告
- `实验X代码.py` — 纯净版代码（不含报告生成逻辑）
- `*.png` — 所有实验生成的图表
- `实验X原理讲解.md` — 教学文档

## 实验类型速查

### 类型 A：机器学习分类/回归实验（如实验三）

典型流程：
1. `from sklearn.datasets import load_xxx`
2. `train_test_split(X, y, test_size=0.2)`
3. 模型 A：`KNeighborsClassifier()` → fit → predict → accuracy
4. 模型 B：`SVC()` → fit → predict → accuracy
5. 调优：循环不同参数，记录准确率，画图对比
6. 混淆矩阵可视化
7. 错误样本分析

必须生成的图：
- 数据样本可视化（如 6 张手写数字）
- 每个模型的混淆矩阵
- 参数调优曲线（如 KNN 的 k 值、SVM 的 C 值）
- 模型对比柱状图（可选）

常见需要贴代码的位置：
- 数据加载代码（`load_digits`、`train_test_split`）
- 模型定义与训练代码（`KNeighborsClassifier().fit()`）
- 预测与评估代码（`accuracy_score`、`confusion_matrix`）
- 错误样本筛选代码（`np.where(y_pred != y_test)`）

### 类型 B：遗传算法优化实验（如实验一）

典型流程：
1. 编码长度计算：`L = ceil(log2((upper-lower)/precision))`
2. 随机初始化种群
3. 解码函数：二进制 → 实数
4. 适应度函数：目标函数
5. 轮盘赌选择
6. 单点交叉
7. 单点变异
8. 迭代 N 代，记录 best/worst/avg
9. 绘制适应度曲线

必须生成的图：
- 适应度随代数变化曲线（best/worst/average）

常见需要贴代码的位置：
- 编码/解码函数
- 轮盘赌选择函数
- 交叉/变异函数
- 主循环代码

### 类型 C：其他数据分析/可视化实验

根据报告要求灵活处理，核心原则不变：模板即需求 → 任务确认 → 精准插入 → 看情况输出结果 → 格式保护 → 教学文档。

## 精准插入 docx 的技术方法

当使用 `python-docx` 向模板插入内容时：

### 策略 A：替换占位符（首选，节省 token）

```python
for para in doc.paragraphs:
    if '（此处贴截图）' in para.text:
        para.text = para.text.replace('（此处贴截图）', '')
        run = para.add_run()
        run.add_picture('image.png', width=Inches(5.0))
    elif '（附核心代码）' in para.text:
        para.text = para.text.replace('（附核心代码）', '')
        run = para.add_run('\n[代码]\n')
        run.font.name = 'Consolas'
        run.font.size = Pt(9)
```

### 策略 B：在段落后插入（次选）

```python
for i, para in enumerate(doc.paragraphs):
    if 'Step 4' in para.text and '图像' in para.text:
        new_para = doc.paragraphs[i]._element
        new_para.addnext(doc.add_paragraph()._element)
```

### 策略 C：fallback（定位失败时）

如果找不到对应位置：
1. 生成一份"内容清单"告诉用户每张图/每段文字应该贴到哪里
2. 同时生成一个"辅助报告"包含所有需要粘贴的内容
3. 不要强行破坏模板格式

## 代码块插入规范

在报告中插入代码时：

```python
from docx.shared import Pt
from docx.oxml.ns import qn

def insert_code_block(paragraph, code_text):
    """在段落中插入等宽字体的代码块"""
    run = paragraph.add_run(code_text)
    run.font.name = 'Consolas'
    run._element.rPr.rFonts.set(qn('w:eastAsia'), 'Consolas')
    run.font.size = Pt(9)
```

## 输出要求

每次完成实验后，向用户呈现：
1. **任务梳理确认单**：从模板中分析出的所有任务（让用户核对）
2. **实现方案**：用了什么数据、什么模型、生成了哪些图、贴了什么代码
3. **核心结果**：如准确率、最优参数、最优解等
4. **文件清单**：归档文件夹内的所有文件及说明
5. **教学文档摘要**：原理讲解文档的核心内容概述
6. **手动注意事项**：如果报告有无法自动填入的部分，明确告知用户

## 注意事项

- **模板即需求**：永远不要忽略报告模板中的任何一句话，"此处贴截图"、"附上代码"都是明确的工作指令
- **先分析后行动**：不要拿到模板就直接写代码，先完整读一遍模板，理解每一步要求
- **任务确认制**：分析完必须让用户确认，避免遗漏（尤其是"贴代码"这种容易被忽略的要求）
- **看情况输出结果**：不要为了填满报告而贴无意义的输出。"加载成功"、"训练完成"这种结果不要贴。
- **格式优先**：宁可手动告诉用户贴图位置，也不要为了自动化而破坏报告格式
- **教学文档**：不要只给结果，要给原理。用户需要的是"懂"，不是"做完"
- **代码分两份**：最终提交的 `.py` 文件里绝对不能有 `from docx import Document`
